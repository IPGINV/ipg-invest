# 🚀 Инструкция по обновлению на сервере

## 📦 Коммит: `fix: улучшена навигация, безопасность и мобильная адаптация`

**Дата обновления:** 2026-02-06  
**GitHub:** https://github.com/IPGINV/ipg-invest

---

## ✅ Что было изменено:

### 🔒 Безопасность:
- Dashboard требует аутентификацию для доступа
- Убрана автоматическая аутентификация через URL параметры
- Очистка токенов при ошибках API

### 🧭 Навигация:
- Исправлены порты всех приложений
- "Личный кабинет" → редирект на Dashboard (страница входа)
- "Калькулятор" → открывает калькулятор в лендинге
- "Проект/Компания" → корректно открывают Info app

### 📱 Мобильная адаптация:
- Отцентрован лендинг для всех мобильных устройств
- Улучшен график золота (55% экрана на мобильных)
- Адаптивные размеры и отступы

---

## 🔄 Пошаговая инструкция для обновления:

### **Шаг 1: Подключение к серверу**
```bash
ssh root@170.168.103.95
```

### **Шаг 2: Переход в директорию проекта**
```bash
cd ~/projects/ipg-invest
```

### **Шаг 3: Получение изменений из GitHub**
```bash
# Проверка текущего статуса
git status

# Получение последних изменений
git fetch origin main

# Просмотр изменений перед слиянием (опционально)
git log HEAD..origin/main --oneline

# Слияние изменений
git pull origin main
```

**Если возникнут конфликты:**
```bash
# Сохранить локальные изменения (если есть)
git stash

# Получить изменения
git pull origin main

# Восстановить локальные изменения
git stash pop
```

### **Шаг 4: Пересборка Docker контейнеров**

**Вариант A: Полная пересборка (рекомендуется)**
```bash
# Остановка контейнеров
docker compose down

# Пересборка образов (без кэша)
docker compose build --no-cache

# Запуск контейнеров
docker compose up -d

# Проверка статуса
docker compose ps
```

**Вариант B: Быстрая пересборка (если нет изменений в зависимостях)**
```bash
# Пересборка и перезапуск только измененных сервисов
docker compose up -d --build
```

### **Шаг 5: Проверка логов**
```bash
# Просмотр логов всех контейнеров
docker compose logs --tail=50

# Просмотр логов конкретного контейнера
docker compose logs dashboard --tail=30
docker compose logs lending --tail=30
docker compose logs info --tail=30
docker compose logs wallet --tail=30

# Отслеживание логов в реальном времени
docker compose logs -f dashboard
```

### **Шаг 6: Проверка работоспособности**

**Проверка контейнеров:**
```bash
docker compose ps
```

Все контейнеры должны быть в статусе **"Up"** или **"healthy"**.

**Проверка API:**
```bash
curl https://api.ipg-invest.ae/health
# Ожидаемый ответ: {"status":"ok","timestamp":"..."}
```

**Проверка приложений:**
```bash
# Lending
curl -I https://ipg-invest.ae
# Ожидаемый ответ: HTTP/2 200

# Dashboard
curl -I https://dashboard.ipg-invest.ae
# Ожидаемый ответ: HTTP/2 200

# Info
curl -I https://info.ipg-invest.ae
# Ожидаемый ответ: HTTP/2 200

# Wallet
curl -I https://wallet.ipg-invest.ae
# Ожидаемый ответ: HTTP/2 200

# Admin
curl -I https://admin.ipg-invest.ae
# Ожидаемый ответ: HTTP/2 200
```

### **Шаг 7: Очистка (опционально)**
```bash
# Удаление неиспользуемых образов
docker image prune -a -f

# Удаление временного файла коммита (если есть)
rm -f .git/COMMIT_MSG.txt
```

---

## 🧪 Тестирование функциональности:

### **1. Проверка безопасности Dashboard**
1. Откройте https://dashboard.ipg-invest.ae
2. Без токена должна показаться форма входа
3. После входа через лендинг → Dashboard откроется с данными

### **2. Проверка навигации из меню**
1. Откройте любое приложение
2. Нажмите гамбургер-меню (☰)
3. Проверьте все пункты:
   - ✅ **Личный кабинет** → Dashboard (страница входа)
   - ✅ **Компания** → Info app (страница "О компании")
   - ✅ **Проект** → Info app (страница "О проекте")
   - ✅ **Калькулятор** → Лендинг (калькулятор)

### **3. Проверка мобильной версии**
1. Откройте https://ipg-invest.ae на мобильном
2. Проверьте:
   - ✅ Текст не выходит за границы экрана
   - ✅ График золота занимает ~55% экрана
   - ✅ Все элементы кликабельны
   - ✅ Меню открывается корректно

---

## 🔧 Устранение проблем:

### **Проблема 1: Контейнер не запускается**
```bash
# Просмотр детальных логов
docker compose logs [SERVICE_NAME] --tail=100

# Перезапуск конкретного контейнера
docker compose restart [SERVICE_NAME]

# Полная пересборка
docker compose down
docker compose build --no-cache [SERVICE_NAME]
docker compose up -d
```

### **Проблема 2: Порт уже занят**
```bash
# Проверка занятых портов
sudo ss -tlnp | grep -E ":(3000|3001|3002|3003|3004|5182|5432|6379)"

# Остановка конфликтующих процессов
sudo kill -9 [PID]
```

### **Проблема 3: Приложение не отображается**
```bash
# Проверка Nginx
sudo nginx -t
sudo systemctl reload nginx

# Проверка Docker сети
docker network inspect ipg-invest_ipg-network
```

### **Проблема 4: Redis/Postgres не работают**
```bash
# Проверка Redis
docker compose exec redis redis-cli ping
# Ожидаемый ответ: PONG

# Проверка PostgreSQL
docker compose exec postgres psql -U ipg_user -d ipg_production -c "SELECT 1;"
# Ожидаемый ответ: таблица с "1"
```

---

## 📊 Мониторинг после обновления:

```bash
# Отслеживание использования ресурсов
docker stats

# Просмотр логов в реальном времени
docker compose logs -f

# Проверка здоровья контейнеров каждые 10 секунд
watch -n 10 'docker compose ps'
```

---

## ⏪ Откат на предыдущую версию (если нужно):

```bash
# Просмотр предыдущих коммитов
git log --oneline -5

# Откат на предыдущий коммит
git reset --hard 341784a  # Замените на нужный хеш

# Пересборка
docker compose down
docker compose up -d --build

# Для восстановления текущей версии
git pull origin main
docker compose down
docker compose up -d --build
```

---

## 📝 Дополнительные заметки:

1. **Backup перед обновлением:**
   ```bash
   # Backup базы данных
   docker compose exec postgres pg_dump -U ipg_user ipg_production > ~/backups/db_backup_$(date +%Y%m%d_%H%M%S).sql
   ```

2. **Проверка свободного места:**
   ```bash
   df -h
   ```

3. **Очистка Docker (если мало места):**
   ```bash
   docker system prune -a --volumes -f
   ```

---

## ✅ Чек-лист успешного обновления:

- [ ] SSH подключение к серверу
- [ ] Git pull выполнен успешно
- [ ] Docker контейнеры пересобраны
- [ ] Все контейнеры в статусе "Up"
- [ ] API /health возвращает 200 OK
- [ ] Все приложения открываются в браузере
- [ ] Навигация из меню работает корректно
- [ ] Мобильная версия отображается правильно
- [ ] Dashboard требует аутентификацию
- [ ] Логи не показывают критичных ошибок

---

## 📞 Поддержка:

Если возникли проблемы:
1. Проверьте логи: `docker compose logs --tail=100`
2. Проверьте статус: `docker compose ps`
3. Попробуйте перезапуск: `docker compose restart`

---

**Обновление завершено! 🎉**
