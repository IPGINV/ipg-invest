# 📱 Обновление: Улучшенное центрирование на мобильных устройствах

## Коммит: `6b73c56`

### ✅ Что изменено:

1. **Увеличены отступы (padding) для мобильных устройств**
   - Main container: `px-6` вместо `px-4`
   - Все секции получили дополнительные отступы

2. **CSS правила для центрирования**
   - Все заголовки автоматически центрируются на мобильных
   - Параграфы центрируются с padding `1rem`
   - Кнопки получают автоматическое центрирование

3. **Предотвращение горизонтальной прокрутки**
   - `overflow-x: hidden` на `html`, `body` и `#root`
   - `box-sizing: border-box` для всех элементов

4. **Адаптивные карточки**
   - Увеличен padding до `p-8` на мобильных

---

## 🚀 Инструкция по обновлению на сервере

### Шаг 1: Подключение к серверу
```bash
ssh root@170.168.103.95
```

### Шаг 2: Переход в директорию проекта
```bash
cd /var/www/ipg-invest.ae/ipg-invest
```

### Шаг 3: Обновление кода из GitHub
```bash
git pull origin main
```

### Шаг 4: Переход в Invest-Lending
```bash
cd Invest-Lending
```

### Шаг 5: Пересборка приложения
```bash
npm run build
```

### Шаг 6: Перезапуск Invest-Lending через PM2
```bash
pm2 restart invest-lending
```

### Шаг 7: Проверка статуса
```bash
pm2 status
```

### Шаг 8: Очистка кэша браузера
```bash
# На сервере (опционально)
pm2 restart nginx
```

---

## 🧪 Проверка после обновления

1. Откройте https://ipg-invest.ae на **мобильном устройстве**
2. ✅ Проверьте, что текст не выходит за границы экрана
3. ✅ Все заголовки должны быть центрированы
4. ✅ Карточки должны иметь достаточные отступы
5. ✅ Горизонтальная прокрутка отсутствует
6. ✅ Проверьте на разных моделях телефонов и ориентациях

---

## 📝 Затронутые файлы

- `Invest-Lending/App.tsx` - обновлены padding и центрирование
- `Invest-Lending/index.html` - добавлены CSS правила для мобильных

---

## ⚠️ Важно

После обновления **обязательно очистите кэш** браузера на мобильном устройстве:
- **Chrome Android**: Settings → Privacy → Clear browsing data
- **Safari iOS**: Settings → Safari → Clear History and Website Data

Или используйте **режим инкогнито** для проверки.
