# ⚡ Быстрое обновление - убраны экраны "Сессия истекла"

## 📦 Коммит: `0f58d24`

### ✅ Что исправлено:
- ❌ Убран экран "Сессия истекла" 
- ❌ Убран экран "Требуется вход"
- ✅ Автоматический редирект на страницу входа
- ✅ Улучшен UX: нет промежуточных экранов

---

## 🚀 Обновление на сервере (3 команды):

```bash
# 1. Подключение и переход в проект
ssh root@170.168.103.95
cd ~/projects/ipg-invest

# 2. Получение изменений
git pull origin main

# 3. Пересборка Dashboard контейнера
docker compose up -d --build dashboard

# 4. Проверка
docker compose logs dashboard --tail=20
curl -I https://dashboard.ipg-invest.ae
```

---

## ✅ Результат:

**Было:**
```
1. Открыть Dashboard без токена
2. Показывается экран "Сессия истекла" ❌
3. Нажать кнопку "Вернуться к порталу"
4. Перезагрузка страницы
5. Показывается форма входа
```

**Стало:**
```
1. Открыть Dashboard без токена
2. Автоматический редирект на форму входа ✅
3. Пользователь сразу видит форму входа
```

---

## 📱 Тест на телефоне:

1. Откройте https://dashboard.ipg-invest.ae на телефоне
2. ✅ Должна сразу открыться страница входа
3. ✅ Без промежуточных экранов

---

**Готово! 🎉**
